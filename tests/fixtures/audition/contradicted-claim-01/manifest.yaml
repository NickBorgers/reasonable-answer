# Matched pair (D-fixture-report-shape): derived from `control-base-dust-bowl-01` by
# appending one paragraph to the final section. Everything else is byte-identical, so a
# critic cannot separate this fixture from its sound sibling by length, structure,
# citation density or topic.
lens: logic
tier: obvious
question: "Did the Dust Bowl migration permanently depress wages in California?"
defects:
  - category: contradicted_claim
    locus: {section: 6, paragraph: 3}
    note: >
      S6.P3 asserts the wage suppression was permanent and that the receiving counties
      never recovered. S6.P2, immediately above it, says those counties no longer stand
      out as a distinct low-wage region by the end of the 1940s, and the conclusion in
      S1.P1 answers the question "no". The contradiction is with the adjacent paragraph,
      not buried across sections.
