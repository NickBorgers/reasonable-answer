.venv/
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/
# run output contains seed material, drafts and critiques — never commit it
runs/

# scratch directories the CI agents write into the workspace
.review-output/
.review-prompt/
.agent-prompt/
